import { PictureDirective } from './picture.directive';

describe('PictureDirective', () => {
  it('should create an instance', () => {
    const directive = new PictureDirective();
    expect(directive).toBeTruthy();
  });
});
